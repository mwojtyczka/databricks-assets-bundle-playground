# This is a Databricks asset bundle definition for marcin_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: marcin_project

artifacts:
  default:
    type: whl
    build: poetry build
    path: .

include:
  - resources/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    default: true
    workspace:
      host: https://adb-8870486534760962.2.azuredatabricks.net
    # default user name to use for all jobs in this environment, otherwise use the principal that deploys the job
    #run_as:
    #  user_name: marcin.wojtyczka@databricks.com

  # The 'dev' target, for development purposes. This target is the default.
  qa:
    # We use 'mode: development' to indicate this is a personal development copy:
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default
    # - The 'development' mode is used for Delta Live Tables pipelines
    mode: development
    workspace:
      host: https://adb-8870486534760962.2.azuredatabricks.net
    # default service principal to use for all jobs in this environment
    run_as:
      service_principal_name: 5a0e4887-c924-4646-9eda-ff8eb38924f3 #sp-databricks-ping

  # The 'prod' target, used for production deployment.
  prod:
    # We use 'mode: production' to indicate this is a production deployment.
    # Doing so enables strict verification of the settings below.
    mode: production
    workspace:
      host: https://adb-8870486534760962.2.azuredatabricks.net
      # We only have a single deployment copy for production, so we use a shared path.
      root_path: /Shared/.bundle/prod/${bundle.name}
    # default service principal to use for all jobs in this environment
    run_as:
      service_principal_name: 5a0e4887-c924-4646-9eda-ff8eb38924f3 #sp-databricks-ping